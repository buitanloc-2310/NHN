# CLOUDFLARE BUILD SETTINGS — NHÀ HÁN NGỮ

Điền đúng:

- Build command: `npm run build`
- Deploy command: `npm run deploy:cloudflare`
- Version command: để trống
- Root directory: `NHN_CTT_PRODUCTION_UI_FIX/NHN_CTT_PRODUCTION`

Không thêm `NHN-main/`, `NHN-main-fixed/` hoặc tên file ZIP vào Root directory.
